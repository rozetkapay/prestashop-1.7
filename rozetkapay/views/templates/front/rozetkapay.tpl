<p class="payment_module">
	<a href="{$urlPayCreat}" title="{$text_title}">
        {if $showIcon }
		<img src="/modules/rozetkapay/img/logo.png" alt="{$text_title}" />
        {/if}
        {$text_title}
	</a>
</p>